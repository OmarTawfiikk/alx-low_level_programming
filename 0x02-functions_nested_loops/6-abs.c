#include "main.h"

/**
 * _abc - function computes the absolute value of an integer
 *
 * @n: takes an integer type input for function
 *
 * Return: always 0 (Success)
 */
int _abs(int)
{
	if (n < 0)
		n = (-1) * n;
	return (0);
}
